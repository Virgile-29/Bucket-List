<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Bucket List{% endblock %}</title>
        <link rel="icon" href="{{ asset('static/images/favicon.png') }}">
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('style/layout.css') }}">
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>


    {% block header %}
            <div class="container header">
                <div class="logo">
                    <img src="{{ asset('static/images/logo.png') }}" />
                </div>
                <div class="title">
                     <h1>Bucket-List</h1>
                </div>
            </div>

    {% endblock %}

    {% block navmenu %}
        <div class="nav-menu">
            <div class="nav-link-item">
                <a class="nav-link" href="{{ path('HomePage') }}">🏠 Accueil </a>
            </div>
            <div class="nav-link-item">
                <a class="nav-link" href="{{ path('wish_list') }}">🧞 Mes souhaits </a>
            </div>
            <div class="nav-link-item">
                <a class="nav-link" href="{{ path('wishForm') }}">🙏 Make a wish </a>
            </div>
            <div class="nav-link-item">
                <a class="nav-link" href="{{ path('Aboutus') }}">ℹ️ À propos </a>
            </div>

        </div>
    {% endblock %}
    {%  block flashMessage %}
        {% for label, messages in app.flashes %}
            {% for message in messages %}
            <div class="alert">
                {{ message }}
            </div>
            {% endfor %}
        {% endfor %}
    {% endblock %}
    {% block body %}
    {% endblock %}

    {% block footer %}
        <div class="footer">
            <div class="trademark">
                <p class="has-text-weight-bold">&copy; Bucket-List {{ 'now'|date('Y')}} </p>
            </div>
        </div>
    {% endblock %}

    </body>
</html>
